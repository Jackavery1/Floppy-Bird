export const SOUND = Object.freeze({
    JUMP: 'jump',
    SCORE: 'score',
    GAME_OVER: 'gameover',
    GROUND: 'ground',
});

export const DIFFICULTY = Object.freeze({
    EASY: 'easy',
    NORMAL: 'normal',
    HARD: 'hard',
});
